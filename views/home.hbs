<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{pageTitle}}</title>
    </head>
    <body>
        {{> header}}
        <p>"message" is an argument to "screamIt()"</p>
        <p>{{screamIt message some}}</p>

        {{> footer}}
    </body>
</html>
